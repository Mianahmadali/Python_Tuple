"""Advanced Level Questions:
Create a tuple of tuples and access a specific nested element.

Given t = ((1, 2), (3, 4), (5, 6)), access the second element of the second tuple.
Convert a tuple into a dictionary.

Given a tuple t = (("a", 1), ("b", 2), ("c", 3)), convert it into a dictionary.
Create a tuple from a list of tuples.

Given the list lst = [(1, 2), (3, 4), (5, 6)], convert it into a tuple of tuples.
Sort a tuple of strings alphabetically.

Given t = ('apple', 'banana', 'cherry'), sort the tuple alphabetically.
Write a function that takes two tuples and returns the tuple with unique elements from both tuples.

Find the union of two tuples, but remove duplicates.
Check if a tuple is a palindrome.

Given a tuple t = (1, 2, 3, 2, 1), check if it reads the same forwards and backwards.
Create a tuple of key-value pairs and extract the keys.

Given t = (('a', 1), ('b', 2), ('c', 3)), extract the keys ('a', 'b', 'c').
Convert a tuple of integers into a tuple of their squares.

Given t = (1, 2, 3), convert it to (1, 4, 9).
Find the symmetric difference between two tuples.

Given t1 = (1, 2, 3) and t2 = (3, 4, 5), find the symmetric difference.
Create a tuple of numbers and calculate the product of all elements.

Given t = (1, 2, 3, 4), find the product of all elements in the tuple.
"""
